<RelativeLayout android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <ImageView
        android:id="@+id/ivFotoLlibreria"
        android:layout_width="match_parent"
        android:layout_height="150dp"
        android:layout_marginHorizontal="12dp"
        android:layout_marginTop="60dp" />

    <TextView
        android:id="@+id/tvDatosLibreira"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="12dp"
        android:layout_marginTop="30dp"
        android:textAlignment="center"
        android:layout_below="@+id/ivFotoLlibreria"/>

    <ListView
        android:id="@+id/lvActividades"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/tvDatosLibreira">

    </ListView>
    <com.google.android.gms.maps.MapView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/lvActividades"
        style="@style/campo">

    </com.google.android.gms.maps.MapView>
</RelativeLayout>